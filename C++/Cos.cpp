#include <stdio.h>
#include <math.h>
int main()
{
  double num;

  printf("Enter a number whose cosine will be found\n");
  scanf("%lf", &num);

  double y;
  y = cos(num);
  printf("Cosine is %lf", sin(num));
}

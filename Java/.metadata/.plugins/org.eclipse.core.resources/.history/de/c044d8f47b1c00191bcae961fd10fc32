package ifelse;
import java.util.Scanner;
public class ifelsevala
{
	public static void main(String[] args)
	{ /*int sum=0,r;
		int n=1276;
		int c=n;
				while(n>0)
				{
					r=n%10;
					sum=sum*10+r;
					n=n/10;
		}
		if(sum==c)
		System.out.println("palindrome no");
		else
			System.out.println("not a palindrome no");
			*/
		String a,b;
		int x=0;
		int g=a.length();
		while (x!=a.length())
		{
			b.charAt(x)=a.charAt(g-x);
			x++;
		}
		
	}
}